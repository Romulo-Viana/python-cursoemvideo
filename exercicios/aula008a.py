import math
n = float(input('Digite um valor: '))
raiz = math.sqrt(n)
print(f'A raiz de {n} é = {raiz}')


from math import sqrt, pow, trunc
n = float(input('Digite um valor: '))
raiz = sqrt(n)
print(f'A raiz de {n} é = {raiz}')